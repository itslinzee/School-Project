Read me file for HW3: Ortiz, Lindsay

This program uses input redirection to read the input file.

To compile:

--  This is a C++ program so g++ is the compiler
--  The following tags must be used for compliation:
    * -std=c++11
    * -fpermisive
    * -lpthread (must be after name of .cpp file)


Example:

g++ -std=c++11 -fpermissive assign3.cpp -lpthread -o myProg
