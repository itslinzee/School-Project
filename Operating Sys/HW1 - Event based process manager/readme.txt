Read me file for HW1: Ortiz, Lindsay

This program uses input redirection to read the input file.

To compile:

--  This is a C++ program so g++ is the compiler
--  The following tags must be used for compliation:
    * -std=c++11

g++ -std=c++11 Event.h Event.cpp main.cpp -o myProg


To run:
./myProg < "input file name here"
